export default function LumaCta() {
  return (
    <section className="mt-12 rounded-lg bg-muted p-8 text-center">
      <h2 className="text-lg font-semibold tracking-tight">
        Need studio-quality audio?
      </h2>
      <p className="mt-2 text-sm text-muted-foreground max-w-md mx-auto">
        Luma removes background noise and turns rough recordings into clean,
        professional audio.
      </p>
      <a
        href="/#waitlist"
        className="mt-5 inline-flex h-9 items-center justify-center rounded-full bg-primary px-6 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90"
      >
        Join the waitlist
      </a>
    </section>
  );
}
